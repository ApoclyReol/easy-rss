# 安全说明

## 数据边界

RSS Reader 是本地应用。论文、订阅源、人工状态、推荐结果和 LLM 配置默认保存在项目内 `data/` 目录：

- `data/rss_reader.sqlite3`
- `data/settings.json`
- SQLite 可能产生的 `-wal`、`-shm`、`-journal` 文件

这些文件已由 `.gitignore` 排除，不能使用 `git add -f` 强行加入仓库。

如果项目目录会被云盘、备份工具或团队同步，仍应把 `data/` 视为敏感本地数据；Git 忽略不等于文件系统隔离。

## 凭据处理

- API Key 只写入本地 `data/settings.json`。
- UI 中以密码输入框显示，并在提示中使用掩码。
- 不要把 Key 放进源码、README、测试 fixture、截图、日志或 issue。
- 不要把真实 `settings.json` 复制成示例文件；如果需要示例，只保留空值和字段说明。
- 发现凭据泄露时，应立即撤销并重新生成，而不是只删除工作树中的文件。

## LLM 数据边界

点击 LLM 复核后，论文标题、摘要、兴趣描述和关键词证据会发送到用户配置的 OpenAI 兼容接口。用户应确认该服务商的数据保留、训练和隐私政策。

关键词模型本地运行，不需要把历史论文标签上传给 LLM。LLM 只复核待判断论文，不会自动处理全部未读。

## 状态与删除安全

- 推荐结果只改变排序和标签，不自动改变 `items.item_status`。
- 批量低相关处理使用可恢复的 `hidden` 状态，不删除论文。
- 过期只是隐藏状态的时间归档，不代表数据删除。
- 删除订阅源或运行数据维护前，应先备份 `data/`。

## 报告安全问题

请不要在公开 issue 中提交 API Key、完整数据库、含个人信息的 RSS 导出或完整错误日志。先移除敏感内容，再描述复现步骤、影响范围和运行环境。
